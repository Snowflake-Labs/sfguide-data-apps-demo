config:
  target: 'http://localhost:3000'
  phases:
    - duration: 60
      arrivalRate: 1
      rampTo: 55
      name: "Warm up"
    - duration: 60
      arrivalRate: 55
      name: "Sustained max load"
scenarios:
  - flow:
    - get:
        url: "/trips/monthly?random=true"
